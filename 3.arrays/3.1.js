let arr = [1, 2, 3, 4, 5]
sum=0
for (let i = 0; i < 
    arr.length; i += 1) {
        sum += arr[i]
    }
    console.log(sum)


    let arr2 = [100, 101, 102]
    sum=0
    for (let a = 0; a < 
        arr2.length; a += 1) {
            sum += arr2[a]
        }
        console.log(sum)